{
  "name": "sierra",
  "version": "0.2.4",
  "description": "Minimalist JavaScript to CSS compiler",
  "main": "./dist",
  "bin": "./bin/sierra",
  "scripts": {
    "test": "tape test/index.js",
    "watch": "watch 'npm run -s test' src test",
    "build": "babel src --out-dir dist",
    "clean": "rm -rf dist",
    "mypublish": "npm run build && npm publish ./"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bgusach/sierra.git"
  },
  "keywords": [
    "css",
    "style",
    "css compiler",
    "css generator"
  ],
  "author": "Bor González Usach <bgusach@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/bgusach/sierra/issues"
  },
  "homepage": "https://github.com/bgusach/sierra#readme",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "tape": "^4.5.1",
    "watch": "^0.18.0"
  },
  "dependencies": {
    "docopt": "^0.6.2"
  }
}
